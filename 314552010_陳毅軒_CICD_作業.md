---
title: "HW 3 - Testing"
author: "陳毅軒"
id: "314552010"
department: "網工所"
---

<!-- create markdown to pdf => alt+m -->
<style>
.text-center{
    text-align: center;
}
.text-left{
    text-align: left;
}
.text-right{
    text-align: right;
}
</style>

<h1><p class="text-center">HW 3 - Testing</p></h1>

<p class="text-center">系所: 網工所　學號: 314552010　姓名: 陳毅軒</p>

## Outline

- [Test Environment](#test-environment)
- [測試描述](#測試描述)
- [Test Case 1：POST 新增 Todo 測試](#test-case-1post-新增-todo-測試)
- [Test Case 2：DELETE 的 if 判斷與 ModifyResult 型別問題](#test-case-2delete-的-if-判斷與-modifyresult-型別問題)
- [測試報告截圖](#測試報告截圖)

---

## Test Environment

| 項目 | 規格 |
|------|------|
| OS | Ubuntu, Kernel 6.17.0-20-generic |
| Node.js | v22.22 |
| 測試框架 | Vitest v4.1.3（含 v8 Coverage） |
| 測試對象 | Backend（Fastify + Mongoose） |
| 測試執行指令 | `npm run test`（`vitest run --coverage`） |

<div style="break-after: page; page-break-after: always;"></div>

## 測試描述

### 測試對象

這次我測的是**後端的 Todo API**。後端是 Fastify + Mongoose，架構分成 Route → Service → Repository 三層。我主要想確認：當 HTTP request 進到後端後，Route 和 Service 最後回傳的 status code 和 response body 是否正確。

### 使用的測試工具

- **Vitest**：專案本來就用這個測試框架，所以我直接沿用。這次主要用 `vi.spyOn` 來 mock Repository，避免真的去連 MongoDB。
- **Fastify `server.inject()`**：用來模擬 HTTP request。這樣測試時不用真的把 server 跑起來，只要直接打 request 進 route 就可以拿到 response。
- **Coverage（v8）**：Vitest 內建的 coverage 工具，用來看哪些程式碼有被測到。

我沒有再裝其他測試框架，因為 Vitest 搭配 `server.inject()` 基本上就可以模擬 request 流程。再加上 `vi.spyOn` 可以把 Repository mock 掉，測試時只需要專注在 route 跟 service 的邏輯。

### 測試策略

這次測試時我把 Repository 層 mock 掉，主要只測 Route 到 Service 之間的行為。原因有幾個：

1. Repository 基本上只是包一層 Mongoose query，邏輯相對單純。但 Route 層還要處理 request 參數、status code 回傳和一些錯誤情況，反而比較容易出 bug。
2. 用 `vi.spyOn` 攔截 Repo 函式，可以控制回傳值（例如回傳 null 模擬找不到資料），不用準備測試資料庫。
3. 測試基本上照著 Arrange → Act → Assert 的順序寫，先準備 mock，再送 request，最後檢查結果。這樣如果測試失敗，也比較容易看出是哪個步驟出問題。

<div style="break-after: page; page-break-after: always;"></div>

## Test Case 1：POST 新增 Todo 測試

### 測試場景

使用者在前端填好 name 跟 description 後，會打一個 `POST /api/v1/todos` 到後端。這個測試主要確認：後端收到 request body 後，是否會正確呼叫 Service 建立 Todo，最後回傳 `201 Created` 和建好的 Todo 物件。

### 測試主體

整條呼叫鏈：Route（`POST /v1/todos`）→ Service（`addTodo`）→ Repository（`createTodo`，被 Mock）。

### 預期結果

| 驗證項目 | 預期 |
|---------|------|
| HTTP Status Code | `201`（Created） |
| Response Body `todo` | 包含 id、name、description、status 完整欄位 |
| `todo.status` | `false`（新建的 Todo 預設未完成） |

### 想驗證的東西

主要確認兩件事：status code 有回 201 而不是 200，以及 response body 裡面有完整的 todo 物件。

201 跟 200 的差別是：201 代表「成功且建立了新資源」，200 只是「成功」。寫 code 的時候其實蠻容易直接寫成 200，所以我覺得這個值得測一下，確保之後有人改 code 不會不小心改掉。

body 的部分，前端拿到 response 之後會直接取 `body.todo` 來用，如果後端回的結構跑掉（比如 key 不叫 `todo` 或少了某個欄位），前端那邊就會出問題。

<div style="break-after: page; page-break-after: always;"></div>

### 程式碼片段

```typescript
test('Given a valid todo body, When receive a POST /api/v1/todos request, '
   + 'Then it should response with status 201 and the created todo', async () => {
    // arrange: mock the repo function to return a created todo object
    const newTodo: Todo = {
        id: '3',
        name: 'todo 3',
        description: 'description 3',
        status: false
    }
    vi.spyOn(TodoRepo, 'createTodo').mockImplementation(async () => newTodo)

    // act: receive a POST /api/v1/todos request
    const response = await server.inject({
        method: 'POST',
        url: '/api/v1/todos',
        payload: { name: 'todo 3', description: 'description 3' }
    })

    // assert: response should be status 201 with the created todo
    expect(response.statusCode).toBe(201)
    expect(JSON.parse(response.body)['todo']).toStrictEqual(newTodo)
})
```

<div style="break-after: page; page-break-after: always;"></div>

## Test Case 2：DELETE 的 `if (todo)` 判斷與 ModifyResult 型別問題

### 測試場景

使用者想刪除一個不存在的 Todo。一般直覺會覺得後端應該回 `404 Not Found`。但寫測試的過程中我發現一個問題：**Route 的 `if (todo)` 判斷對 DELETE 其實不會走到 else 分支**。

### 測試主體

Route（`DELETE /v1/todos/:id`）→ Service（`deleteTodo`）→ Repository（`deleteTodoById`，被 mock 成回傳 `{ value: null }`）。

### 預期結果

| 驗證項目 | 預期 |
|---------|------|
| HTTP Status Code | `204`（不是 404） |

### 想驗證的東西

一開始我想照 PUT 404 的寫法來寫 DELETE 404 的測試。但在讀 code 的時候注意到 `deleteTodoById` 的回傳型別跟 `updateTodoById` 不一樣——PUT 那邊回的是 `Todo | null`，但 DELETE 這邊回的是 `ModifyResult<Todo>`。

我去 `node_modules` 裡看了一下 Mongoose 的型別檔，看到 `ModifyResult` 的結構大概是 `{ value: T | null, ok: ... }` 之類的。也就是說，`findByIdAndDelete` 找不到文件的時候，回傳的不是 null，而是 `{ value: null }`——這是一個物件，所以在 `if` 判斷裡會被當成 truthy。

這樣 Route 的 `if (todo)` 拿到 `{ value: null }` 就會判斷成 true，走到 204。PUT 不會有這個問題，因為找不到就是回 null，`if (null)` 正常走到 else 回 404。

所以我 mock 的回傳值改成 `{ value: null }` 而不是 null，assert 也改成 `expect(statusCode).toBe(204)`。這裡用了 `as any` 是因為 `{ value: null }` 不完全符合 `ModifyResult<Todo>` 的完整型別，TypeScript 會報錯，加 `as any` 讓它過。

看起來 Route 的 `if (todo)` 可能應該改成 `if (todo?.value)`，不然 DELETE 的 404 分支實際上走不到。

我一開始只是想補 DELETE 404 的測試，但寫到一半才發現 Route 的判斷邏輯其實有問題。這次寫測試的過程也讓我重新去看了一次 Route 的實作。

<div style="break-after: page; page-break-after: always;"></div>

### 程式碼片段

```typescript
test('Given an invalid ID, When receive a DELETE /api/v1/todos/:id request, '
   + 'Then it should response with status 204 due to truthy ModifyResult', async () => {
    // arrange: mock repo to return { value: null } — ModifyResult is truthy even when doc not found
    vi.spyOn(TodoRepo, 'deleteTodoById').mockImplementation(
        async () => ({ value: null }) as any
    )

    // act: receive a DELETE /api/v1/todos/:id request
    const response = await server.inject({
        method: 'DELETE',
        url: '/api/v1/todos/nonexistent-id'
    })

    // assert: { value: null } is truthy, so Route returns 204 instead of 404
    expect(response.statusCode).toBe(204)
})
```

<div style="break-after: page; page-break-after: always;"></div>

## 測試報告截圖

### Vitest 測試結果與 Coverage 覆蓋率報告

測試執行指令：`cd backend && npm run test`

<figure style="text-align: center; margin-bottom: 2em;">
<img src="test.png" alt="Vitest 測試結果與 Coverage 覆蓋率報告" style="width: 100%;" />
<figcaption>Vitest 測試結果與 Coverage 覆蓋率報告（3 files, 12 tests passed）</figcaption>
</figure>

截圖中可以看到目前的 coverage 狀況：

| 檔案 | Stmts | Branch | Funcs | Lines |
|------|-------|--------|-------|-------|
| `src/routes/todo.ts` | 73.52% | 75% | 100% | 73.52% |
| `src/services/todo.ts` | 100% | 100% | 100% | 100% |
| 整體 | 74.32% | 90% | 65% | 77.46% |

### 未覆蓋行數

Route 還有一些行沒被覆蓋到：**19-20, 30-31, 46-47, 58-62**。

大部分是各個 endpoint 的 `catch` block（19-20, 30-31, 46-47, 61-62），因為我沒有模擬 Service 拋錯的情境。這些 catch 主要是處理像 MongoDB 斷線之類的錯誤狀況，要測的話需要讓 mock 丟 `throw new Error(...)`，再檢查 Route 是不是回 500。

第 58 行是 DELETE 的 404 路徑，跟前面 Test Case 2 講的一樣，因為 `ModifyResult` 是 truthy 所以走不到。


